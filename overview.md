# 《敕造纪元·持牌者》· 设计文档包交付总览

> 剑与魔法中世纪 · 单机网页 RPG · 复用《守夜人·凋零纪元》代码骨架
> 交付日期：2026-08-02 ｜ 阶段：Phase 1–3 完成，文档先行阶段收口

---

## 1. 项目定义

| 维度 | 决策 |
|---|---|
| 类型 | 挂机 / 半自动战斗 RPG（对标前作守夜人） |
| 题材 | 剑与魔法中世纪 —— **魔法官僚制** |
| 架构 | **纯单机**：单文件 `index.html` + localStorage 多槽位存档，零服务端 |
| 复用策略 | 骨架复用 + 内容全新（79% 工作量是填数据表，非写系统） |
| 评审强度 | Lean |

**世界观钩子**：魔法没有被消灭，它被上了锁、编了号、发了牌。玩家是**持牌回收者**——拿执照的缉私者，靠查封无印魔物与法器过活。

这个钩子的价值在于，它给骨架里每一个"因为是游戏所以这样"的机制都配了叙事解释：

| 机制 | 叙事解释 |
|---|---|
| 自动战斗 | 印记按注册协议自动释放法术 |
| 疯狂捡装备 | 你是来**查封**的，这是工作 |
| 分解装备 | 违禁品**依法熔毁**，熔出印屑 |
| 稀有度五档 | 那是**认证等级**（无印/注籍/批准/特许/敕造） |
| 打 Boss | 那是**通缉名单**上的人 |
| 甲型选择 | 三势力**站队表达**（王庭板甲 / 秘典庭布甲 / 私法皮甲） |

---

## 2. 交付清单（17 份）

### 概念层
| 文件 | 内容 |
|---|---|
| `design/concept/game-concept.md` | 设计支柱 ×3、MDA 分析、核心循环、世界观骨架、五势力据点、Boss 名录、与守夜人的逐项差异化对照、MVP/V1/Later 范围分层 |
| `design/concept/system-breakdown.md` | A–G 七组系统清单，逐条标注 沿用/改造/新增/移除 |

### 美术层
| 文件 | 内容 |
|---|---|
| `design/art/art-bible.md` | 视觉身份九节、双层底色体系、16 类 body 枚举、可访问性 Standard 基线 |
| `design/art/contrast-audit.md` | WCAG 对比度实测底稿（全部为实测值，非目测估算） |

### 系统设计层
| 文件 | 内容 |
|---|---|
| `design/gdd/01-character-progression.md` | 5 属性、5 出身、等级曲线、派生公式、战力估算面板 |
| `design/gdd/02-equipment-affix.md` | 5 阶认证、13 武器、3 甲型、15 词缀、套装、Boss 唯一装备 |
| `design/gdd/03-skills.md` | ~25 技能全表、武器绑定矩阵、光环、禁咒、学派裁决 |
| `design/gdd/04-combat-monsters.md` | 战斗循环参数、27 怪全表、五族群技能分池（v1.1，经返工） |
| `design/gdd/05-economy-loop.md` | 三图配置、掉落、印屑、锻造、金币出口模型、药剂、离线收益 |

### 技术层
| 文件 | 内容 |
|---|---|
| `docs/architecture/00-singleplayer-migration.md` | 单机化剥离手术方案（S1–S5 + S3.7/S3.8，含 DoD grep 清零清单） |
| `docs/architecture/01-main-architecture.md` | 单文件内部分区、模块边界、状态管理、localStorage 容量方案 |
| `docs/architecture/02-offline-progress.md` | 离线收益接口、时间回拨三道闸 |
| `docs/architecture/adr/ADR-001` | 单文件 vs 模块化（记录既定决策与代价） |
| `docs/architecture/adr/ADR-002` | 存档 schema 与版本迁移策略 |
| `docs/architecture/adr/ADR-003` | 数据表与逻辑分离 |
| `docs/architecture/adr/ADR-004` | Auth 守卫剥离契约 |
| `docs/architecture/adr/ADR-005` | 渲染策略：全量重绘与三护栏 |

---

## 3. 关键决策记录

| # | 决策 | 依据 |
|---|---|---|
| D1 | 稀有度取 **5 阶**，语义为认证等级 | `RARITY` 的 `affixes:0-4` 与 `mult:1.0-2.25` 与阶数强绑定，加阶要重算曲线并撑爆 15 条词缀池 |
| D2 | 护甲取 **3 类**，锁甲入 Later | 骨架 `ARMOR_TYPES` 正好 3 类，与三势力 1:1 咬合；加第 4 类会打断"甲型即站队"的映射 |
| D3 | 主底色 **双层折中** | 战斗区冷石造暗底 + UI 面板羊皮暖底，兼顾沉浸感与"冷光＝违禁魔法"的语义编码 |
| D4 | 稀有度色**强制双变体** | 实测证明不存在单一 HEX 能在两域同时过 4.5:1（石区需 L≥0.3121，纸区需 L≤0.1021，区间不相交） |
| D5 | 稀有度主传达通道改为**盾形角饰计数** | 相邻阶色彩对比度仅 1.01–1.36:1，颜色无法可靠排序；颜色降为辅助、文本兜底 |
| D6 | 学派与三系**正交双标签** | `attr` 进数值公式，`school`（9 元素）仅表现与叙事。做元素克制需新增 243 抗性值且改核心公式，而自动战斗中玩家无法响应克制 |
| D7 | MVP 补第二金币出口**「登记费」** | 锻造+重铸仅吃掉 30–50% 活跃金币，净结余 +5.9k/hr。登记费按装备品质累进（800/3000/10000），同时兑现支柱三"越强越违法" |
| D8 | 离线收益**要防时间回拨**（仅向前跳） | 单机不需防作弊，但需防玩家误改系统时间导致收益爆炸损坏存档。三道闸以硬上限为主，成本约 5 行 |
| D9 | localStorage **精简序列化，不引压缩库** | 最坏场景实测 6.38MB 超 5MB 配额；三项优化后压至 1.35MB（配额 27%）。引压缩库会推高单文件体积与调试难度 |

---

## 4. 质量门记录

### P1 概念阶段 · CONCERNS → 已解决
四处跨成员冲突，两处由骨架事实裁定（D1/D2），一处交用户拍板（D3），一处留 Phase 2 澄清（D6）。

### P2 系统设计阶段 · **FAIL → 已修复**
`04-combat-monsters.md` v1.0 判定不合格，三处根因：

| # | 问题 | 证据 |
|---|---|---|
| ① | **换皮源作地图** | 第 2 组冰蓝雪原怪、第 3 组虚空紫怪，实为守夜人「雪原→虚空」两图换皮，且与 `05-economy-loop.md` L52–54 的地图配置自相矛盾 |
| ② | **图 2 Boss 走失** | 应为「三指」奥兹（人），实写为"黑市之主"`wraith` 冰色带光环 |
| ③ | **人型 family 里没有人** | 4 只标注人型的怪全用 `body: imp`，且其中两只是渡鸦与蜘蛛；根因是自定 10 类 body 枚举否决了美术已交付的 16 类，缺 `humanoid_civil` |

**修复验证**（团队自述之外的独立核查）：
- ✅ 守夜人词汇已清除，仅存两处出现在"已修正"变更记录中
- ✅ 图 2 全部改为 `humanoid_civil` + 四种 `garb_*` 装束（雇佣兵/私法术士/走私犯/黑市掮客），配色由冰蓝改为市集暖褐
- ✅ 三 Boss 对齐概念文档：锈冠牡鹿 / 「三指」奥兹 / 缄默圣女·艾德娜
- ✅ **数值骨架原样保留**——逐条比对确认（如走私犯 `hp100/atk16/def16/spd24/crit.04` 与 v1.0 对应怪完全一致），返工只动主题层

### P3 技术阶段 · PASS
工程独立复核推翻策划勘察 **5 处**，见下节风险 R1。

---

## 5. 已知风险与缓解

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| R1 | **`castSkill()` L2708 护盾静默失效** —— ✅ **已源码实测实锤**：护盾计算与 `sendPB()` 确在**同一行**，整行删除即静默失效。战斗照常运行、日志正常，只表现为玩家莫名暴毙 | 🔴 高 | ADR-004 剥离契约 + DoD grep 清零 + R14–R19 担保 |
| R2 | **`matAdd` 误删导致印屑闭环断链** —— `matAdd` L2133 与分解调用 L2150 必须保留，只删 L2917 掉落调用。断链在剥离阶段测不出来，需玩家玩到分解那步才暴露 | 🔴 高 | S3.7 + R17.1 已锁定 |
| R3 | **`MMO` 悬空引用致战斗硬崩** —— ✅ 实测：`startBattle` L2492 原文 `if (MMO && MMO.party && ...)`，**首 token 是裸 `MMO`**。删 MMO 块后若残留此行，每次开战抛 `ReferenceError`，**全部战斗不可用**。另 `partyMode` 实测 13 处（初版勘察写 5 处，低估） | 🔴 高 | DoD 必须搜裸标识符 `\bMMO\b`——只清 `MMO.xxx` 会留下悬空引用。已修正 `system-breakdown.md` G1–G3 |
| R4 | **技能优先级重排** —— `buff`/`heal` 无 `mult` 字段导致排序值取 0，永远排在伤害技之后；MVP 的 11 个非伤害技中 **8 个实质永不释放**。修复约 15 行代码 | 🟡 中 | `GDD 03 §3.3` 为权威规格（已完整记录变更前后顺序与根因）；迁移文档 S3.8 承载执行，RK-8 单独跟踪；**不单开 ADR**，不计入剥离 DoD grep 清零 |
| R5 | **登记费平衡未经实测**（GDD 05 H2） | 🟡 中 | 需垂直切片实测后调参 |
| R6 | **`lifesteal` 仍可能超模** —— 已加 3% 回血上限，但未实测 | 🟡 中 | 同上 |
| R7 | **纯单机的 Relatedness 缺口** —— 砍掉社交与 PvP 后，MVP 只能靠 Boss 拟人化硬撑 | 🟡 中 | V1 必须上 AI 佣兵 + 图鉴 |
| R8 | **换皮惯性** —— 骨架复用最省力的路径就是连内容一起复用。本轮已在怪物表上暴露一次并拦下 | 🟡 中 | 实施阶段每个数据表交付时对照概念文档 §6–§7 复核 |
| R9 | **Auth 静默守卫致存档不写 / 界面全白** —— ✅ 实测 5 处，最毒的是 **L3402 `save()`** 与 **L3654 `renderAll()`**，均为**静默 return**：不处理则存档写不进去、界面整片空白，且**零报错**，从控制台完全看不出原因 | 🔴 高 | ADR-004 已准确覆盖；剥离时逐处摘除守卫而非绕过 |

---

## 6. 下一步：Phase 4 预制作

尚未开始，建议顺序：

> 🔴 **开工前必读 · 源码不在本工作区**
> 当前目录下 23 个文件全是设计文档，**零 `.html`**。所有技术文档里的行号引用（`matAdd` L2133、掉落调用 L2917、`castSkill` L2708、`unitAct` L2633、`enemyUnit` L2455 等）指向的都是**前作源码**：
> `C:\Users\Biostar\WorkBuddy\2026-07-31-23-14-34\nightwatch\index.html`
> 引用本身经核对无误。迁移文档已钉死**绝对路径 + 4831 行**为行号有效性基准，并规定 **S0 第 0 步校验**：开工首步必须先比对源文件**字节数 / SHA256 一致且行数 = 4831**，通过后才允许动代码。SHA256 基准值留作开工首步补算回填（当前沙箱无法稳定读取跨盘哈希，已在文档中标注）。
> ⚠️ 若源文件在此期间被改动过，全部行号引用即刻失效，必须重新锚定后再执行删除集。
>
> ✅ **锚点已源码实测复核（8/8 全中）**：行数 4831 ✓ ｜ `matAdd` L2133 ✓ ｜ `decompActual` L2150 ✓ ｜ S3.7 五行删除集（2850 / 2854 / 2917 / 2918 / 4480）✓ **经全文扫描确认无漏网** ｜ `enemyUnit` L2455 公式与 GDD 04 逐字符一致 ✓ ｜ `partyMode` 13 处 ✓ ｜ Auth 守卫 5 处 ✓
> 结论：**Phase 4 的地基是实的，不是文档推断**。

1. **先做单机化剥离并验证**（工程，阻塞项）—— 验收：离线可开、可创角、可存读档、断网零报错、控制台无重连日志。**这一步不过，后面所有内容工作都建在会塌的地基上。**
2. UX 规格（策划，GDD 05 F8）：开局/创角屏、地图屏、战斗屏、背包决策屏
3. 资产规格（美术）：ICONS 清单落地、16 类 body 的 path 实现规范
4. Epic / Story 拆分与测试框架脚手架（工程）
5. 首个冲刺计划汇编

---

## 6.5 协作前提（三条硬规矩 · Phase 4 强制生效）

本轮发生过三次同源事故，全部源于**按消息转述动工，而非按文件现行版本动工**：

| # | 事故 | 代价 |
|---|---|---|
| 1 | 策划自定 10 类 body 枚举，否决美术已交付的 16 类 | 整份 GDD 04 返工 |
| 2 | 主理人的 ADR-006 裁定被静默推翻，未回传 | 收口文档出现幽灵交付项 |
| 3 | 美术依据**已作废的** GDD 04 v1.0 条款，撤回自己正确的成果 | art-bible 倒退，守夜人原怪（含错误 Boss「黑市之主」）写回，几乎抵消整轮返工 |

故立以下三条为强制规矩：

1. **跨成员传递枚举 / 数值 / 命名，一律只给「文件路径 + 章节号」，禁止在消息里内联清单。**
   内联清单会随时间过期，路径与章节号永远指向当前版本。
2. **收到任何"某文档规定了 X"的转述时，动工前必须打开该文件确认现行内容**——包括主理人发出的转述。
   **消息是快照，文件是现状；两者冲突时永远以文件为准。**
3. **每份文档头必须有版本号与变更摘要。**
   若 art-bible 与 GDD 04 都能一眼看出"我是第几版、上版改了什么"，事故 3 在动工前就会被察觉。

补充一条验收原则：**交叉验，不自验。** 污染源的制造方不得签收自己那条链路的清理结果；同理，主理人的收口文档需由成员抽查（本轮的幽灵 ADR-006 正是这样被抓到的）。

---

## 7. 遗留事项

- `design/art/` 下有 4 个临时计算文件（`_contrast*.txt`、`_ls.txt`，共 356 字节）因环境安全删除组件的路径转换缺陷无法清除，内容已失效，可手动删除，不影响交付。
- 音频方向（`audio-director`）本轮未启动，属 Phase 6 打磨阶段范围。
- 源码 **L3597–3600** 是守夜人的旧档迁移死代码。Arcanum 从零开始无旧档，剥离时可一并清除（低优先级，不影响功能）。
